#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    if (argc == 1) {
        //no input, so enter interactive mode
        return 0;
    } else if (argc == 2) {
        //input give, excutes the given file
        return 0;
    }
}